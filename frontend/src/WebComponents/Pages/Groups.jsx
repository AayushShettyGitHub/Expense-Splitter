import React from "react";
import Layout from "@/WebComponents/Pages/Layout";
import MyGroups from "@/WebComponents/SideBarComponents/MyGroups";

const Groups = () => {
  return (
    <Layout pageTitle="My Groups">
      <MyGroups />
    </Layout>
  );
};

export default Groups;
